{
  "name": "sinan-robo",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Sinan Robo · 司南·机脑 —— 开源具身模型（VLA）的可审计索引、适配矩阵与延迟/成本层",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run import && npm run validate && astro build",
    "preview": "astro preview",
    "import": "node scripts/import-seed.mjs",
    "validate": "node scripts/validate.mjs",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name sinan-robo"
  },
  "dependencies": {
    "astro": "^5.12.0",
    "zod": "^3.23.8",
    "@astrojs/sitemap": "^3.4.0"
  }
}